# 🛠 Day 1: Terminal & CLI Fundamentals

### 📂 Navigation
* **`cd`** (Change Directory): Used to move between folders.
* **`ls`** (Listing): Shows all files and folders in the current path.

### 🏗 Creation & Deletion
* **`mkdir`**: Create a new directory.
* **`touch`**: Create a new file (e.g., `touch index.html`).
* **`rm`**: Delete a file.
* **`rm -r`**: Delete a directory and all its contents (Recursive).
* **`rm -ir`**: Delete with a confirmation prompt (Interactive/Caution).

### 📋 Organization
* **`mv`**: Used to **move** files or **rename** them.
* **`ls -R`**: Checks the directory structure recursively (shows all sub-folders).

---

### 🖥 Practice Output
**Project Structure:**
```text
project/
├── config/
│   └── config.json
├── src/
│   ├── app.js
│   └── styles.css
└── README.md
